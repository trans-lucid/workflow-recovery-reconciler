[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "workflow-recovery-reconciler-template"
version = "0.1.0"
description = "Internal Translucid workflow recovery reconciler challenge template"
requires-python = ">=3.12"
dependencies = []

